<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="DIYHIIT.Views.Tabs.ProfileView">

     <ContentPage.ToolbarItems>
        <ToolbarItem Text="Log out"
                     Command="{Binding LogoutCommand}"/>
    </ContentPage.ToolbarItems>

    <ContentPage.Content>
        <StackLayout HorizontalOptions="Center"
                     VerticalOptions="CenterAndExpand">

            <Label Text="{Binding TextLabel}"/>
            <Button Text="Test User API Post"
                    Margin="20"
                    Padding="20"
                    Command="{Binding TestPostCommand}"
                    HorizontalOptions="CenterAndExpand"
                    VerticalOptions="CenterAndExpand"
                    FontSize="Large"
                    BackgroundColor="LightSalmon"
                    TextColor="Blue"
                    CornerRadius="10"/>
            <Button Text="Test User API Get"
                    Margin="20"
                    Padding="20"
                    Command="{Binding TestGetCommand}"
                    HorizontalOptions="CenterAndExpand"
                    VerticalOptions="CenterAndExpand"
                    FontSize="Large"
                    BackgroundColor="LightCyan"
                    TextColor="Black"
                    CornerRadius="10"/>

        </StackLayout>
    </ContentPage.Content>

</ContentPage>
